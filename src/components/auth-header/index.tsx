import React from 'react'

import Logo from '@components/logo'

import './style.scss'

const blk = 'auth-header'

const AuthHeader = () => (
  <div className={blk}>
    <Logo />
  </div>
)

export default AuthHeader
